apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  namespace: postgres
spec:
  instances: 1 # single node home lab - increase for HA later

  postgresql:
    parameters:
      max_connections: "200"
      shared_buffers: "256MB"
      effective_cache_size: "768MB"
      maintenance_work_mem: "64MB"
      checkpoint_completion_target: "0.9"
      wal_buffers: "16MB"
      default_statistics_target: "100"

  bootstrap:
    initdb:
      database: authentik # first database created automatically
      owner: authentik # owns that database
      secret:
        name: postgres-authentik

  # Superuser external login. CNPG defaults this to false (postgres user locked
  # in pg_hba.conf) for safety. Enabled here so admin tasks (CREATE DATABASE,
  # CREATE EXTENSION, GRANT) can be run from outside the cluster against the
  # LoadBalancer service. Authentik and other workloads should use their own
  # non-superuser accounts; this is only for human admin work.
  enableSuperuserAccess: true
  superuserSecret:
    name: postgres-superuser

  storage:
    size: 10Gi
    storageClass: local-path # k3s built-in storage class

  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

  monitoring:
    enablePodMonitor: false # set true when you add Prometheus later
