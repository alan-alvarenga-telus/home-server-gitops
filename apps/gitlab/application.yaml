# apps/gitlab/application.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitlab
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: apps
  source:
    repoURL: https://github.com/alan-alvarenga-telus/home-server-gitops.git
    targetRevision: HEAD
    path: apps/gitlab/overlays/home-server
  destination:
    server: https://kubernetes.default.svc
    namespace: gitlab
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  # No ServerSideApply here (unlike postgres/authentik): this app has no
  # CRDs and nothing large enough to need it. It was also actively
  # counterproductive - ArgoCD's serverSideDiff mode (paired with
  # ServerSideApply) does not honor ignoreDifferences below for a field
  # nested inside spec.volumeClaimTemplates, keeping this Application stuck
  # OutOfSync forever even though the live StatefulSet exactly matched git.
  # Plain client-side diffing does honor it correctly.
  #
  # Kubernetes stamps a status.phase onto every StatefulSet
  # volumeClaimTemplates entry in the live object, even though a template has
  # no business carrying status. Git never sets it (there's nothing to set),
  # so without this it's permanent, un-fixable drift - not a real config
  # mismatch.
  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      jqPathExpressions:
        - '.spec.volumeClaimTemplates[]?.status'
